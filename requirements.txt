# Tooling dependencies. scripts/fetch_sources.py needs none of these — it is
# standard library only.

# Source integrity and metadata only (scripts/verify_sources.py). Everything
# else that reads a PDF uses pymupdf.
pypdf>=6.0

# Page-by-page text and layout extraction, and the page -> PNG rendering behind
# accuracy rule 6: extract_figures.py crops with page.get_pixmap(). No poppler
# or system package required.
pymupdf>=1.26

# Compliance matrix export (scripts/build_matrix.py). The matrix is derived from
# the vault notes, so it is regenerated, never hand-edited.
openpyxl>=3.1
