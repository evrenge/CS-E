# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Build / generated output
build/
out/
dist/
*.tmp

# Note: deck/ is NOT ignored. deck/compliance_matrix.xlsx is derived from the
# vault but committed, so a reviewer gets it without running the pipeline.
# Regenerate it with scripts/build_matrix.py; never hand-edit it.
